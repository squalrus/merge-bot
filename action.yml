name: 'Pull Request Merge'
description: 'Merge a pull request after reviewed and labeled'
inputs:
  test:
    description: 'Runs in test mode and will comment rather than merge'
    required: false
    default: false
  labels:
    description: 'Labels required for integration'
    required: true
    default: 'ready'
  reviewers:
    description: 'Reviewers required and reviewers must sign-off'
    required: false
    default: true
runs:
  using: 'node12'
  main: 'index.js'
